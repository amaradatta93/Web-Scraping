In order to run the answers:

1. Create a python3 virtualenv `virtualenv env`
2. Activate virtualenv `source env/bin/activate`
3. Install requirements `pip install -r requirements.txt`
4. run `python answers.py`
